# NetDiag Twin 0.5.4 版本计划与缺陷台账

登记日期：2026-09-12。目标版本：**0.5.4**。

用户要求：**下个版本解决所有现有已知 bug，记作 0.5.4。**

追加要求（2026-09-12 至 2026-09-13）：依赖升级至最新稳定版，处理必要的接口迁移；上游固定的
兼容约束须提供依赖链证据，安全补丁须满足上游替代条件后才可移除。

版本定位为缺陷修复与回归验证。所有已确认缺陷均须修复并验证，严重级别只决定
处理顺序，不作为延期或豁免依据；修复期间新发现的缺陷和回归同样纳入本版。
当前状态为**已确认缺陷修复及本机复核完成，正式发布验收进行中**。
正式发布、旧版更新和原始数据迁移尚须按下文完成，本文不是发布完成公告。

## 登记基线

- 登记时工作区版本为 `0.5.3`；执行阶段已将候选包版本升级到 `0.5.4`，
  分支为 `lza/v0.5.4-known-bugs-and-dependencies`，尚未发布。
- 检查的本地提交：`74b1bd11a8b7b62381344e9aae1b5baa707cce4f`
  （`release/v0.5.3`），与远端 `main` 提交
  `968e9bb051311655920234c29e961cfdac571bac` 的文件树无差异。
- 登记时 GitHub 最新正式发行版仍为
  [v0.5.2](https://github.com/billlza/netdiag-twin/releases/tag/v0.5.2)。
- 上述 `main` 的 [CI 运行](https://github.com/billlza/netdiag-twin/actions/runs/29580409311)
  于 2026-07-17 创建并已成功；它只证明该提交的历史 CI 结果，不能充当 0.5.4
  候选、安装、更新或真实设备验收。

本次初始盘点读取了仓库文档、全部 5 条开放 issue、最近合并的 PR #8 审查意见和
近期 main 工作流记录。它不是完整代码审计或端到端复测。下列待复核项须逐项给出
结论，不能把“没有复现”“已有实现”或“issue 很旧”直接当成已修复。

## 已确认缺陷

| 编号 | 问题与证据 | 状态与关闭证据 |
| --- | --- | --- |
| D-01 | [短报告](short_report.md)仍称交付物为 Streamlit 应用，并引用不存在的 `setup_env.sh`；实际入口是 `crates/netdiag-app/src/main.rs` 的 `eframe::run_native`。 | 本次已修正文档：改为 Rust 桌面端与 CLI、现有 Cargo 清单和质量门禁入口；示例数值标明为演示而非实测。按文件与入口核对关闭。 |
| D-02 | Overview 五卡宽度在非整除空间下不同：旧实现把像素余量全部交给最后一张。`layout.rs` 新增测试在宽度 772 时证明旧实现失败。 | 已修复并通过 5 项布局回归；原始证据：`overview-width-red.log`、`overview-layout-green-02.log`。本轮原生中英文、窗口放大及键盘缩放检查通过。 |
| D-03 | 小窗口 Overview 滚动状态变化后仍按外层窗口坐标绘制，内容不随滚动移动。 | 已改用滚动子 UI 坐标和可用宽度；`overview-scroll-red-03.log` 为旧行为失败，`overview-layout-green-02.log` 为修复后通过。 |
| D-04 | 真实 macOS `system-counters` 的 `all`/`lo0` 采集失败；解析器混入 IPv4/IPv6 地址别名行，并在链路地址为空时错列读取计数。旧回归把 bytes=3000 错读为 20。 | 已修复；17 项计数器回归、最终完整 strict 和 `all`/`lo0` 真实 CLI 通过。真正缺失/损坏/溢出的接口计数仍拒绝；证据见 `native-collector-final-receipt.json`。 |
| D-05 | 系统计数器和 pcap 把未采集的超时、重试、DNS/TLS 失败事件标为实测；系统接口错误比例被标为实测端到端丢包；RTT 的 0.1 ms 占位值被 warning 错报为 0.0。 | 已修复并通过最终完整 strict；真实系统计数器及合成 pcap 的 CLI 均报告 1 measured / 1 estimated / 8 fallback。旧行为失败保存在 `netstat-provenance-red.log`、`pcap-provenance-red.log`、`counter-estimate-red.log`。 |
| D-06 | 现有历史报告的窗口延迟仅保存 mean/P95/std，读取器强制要求 P50/P99，导致历史读取及显式根迁移失败。文件没有生产版本字段，未断言其具体来源版本。 | 已将缺失分位数表达为未记录，图表保留空缺；新分析仍计算全部分位数。旧行为失败见 `legacy-window-red.log`、`legacy-report-migration-red.log`；40 项相关回归及 strict-11 通过。 |
| D-07 | 0.5.2 写入绝对 run/index 和 manifest 路径，新版一律拒绝，导致合法旧数据无法迁移。已用历史代码和现有数据副本复现。 | 修复限于当前根和 run 目录内部的绝对路径；外部根、父级跳转和符号链接越界仍拒绝。旧行为失败见 `legacy-absolute-path-red.log`；57 项回归及 strict-09 通过，副本 460 个既有文件字节不变，50 条历史记录可读。 |
| D-08 | 未配置 API URL 的合法初始状态在设置页显示 Keychain 错误；端点校验失败被混同为凭据读取失败。 | 设置表单的空地址明确表示未配置，不查询 Keychain；无效非空地址仍报端点错误，真实凭据读取失败仍报 Keychain 错误。运行和写凭据入口继续要求有效地址。相关回归、strict-11 及原生中英文设置页复验通过。 |
| D-09 | 英文概览放大后，诊断标题/置信度和状态标签/值发生重叠；双列比较继承强制两端对齐，换行时字距异常。 | 标题/置信度及状态标签/值分行，比较文字恢复自然间距；`compact-ui-red-03.log` 复现三个旧行为失败，`gui-regressions-green-01.log` 全部通过。 |
| D-10 | 用户选择工作区内的数据目录后重启，bundle 规范化把它误改为默认目录；旧逻辑把任意工作区后代目录都视为历史默认值。 | 显式选择记录在设置中并跨重启保留，自动迁移仅识别未显式选择的工作区直属默认目录；持久化/重载、嵌套目录、strict-11 及原生退出后重启复验通过。 |
| D-11 | 旧模型的运行读取器拒绝 v1，而现有显式迁移只能伴随重新训练或合成模型重建，无法保留当前模型作为选中版本。 | 已增加 `netdiag model migrate`，复用串行、持久化模型发布边界；21 项相关核心回归、38 项 CLI 测试及 strict-13 通过。真实旧数据第三份副本保持权重字节和已有训练元数据值，重复迁移不改文件，50 条历史可读、后续诊断及签名 App 启动成功。 |
| D-12 | JSON 默认浮点解析使持久化阈值在重新读取时偏移一个精度单位，例如 `40.100254306676476` 变成 `40.10025430667648`，迁移元数据精确相等回归因此失败。 | `model-migration-regressions-02.log` 保留失败；启用 serde_json 的 `float_roundtrip` 后，同一精确相等断言及 strict-13 通过。没有改成近似相等或放宽阈值。 |

后续确认的 bug 追加到本节。每项保留受影响版本/平台、期望与实际行为、复现输入、
根因位置、修复文件或提交、验证命令和结果、证据路径及剩余限制。

## 必须复核的历史事项

以下 issue 登记时均为开放状态，未标注 bug 标签。它们包含历史功能和验收任务，
是否仍存在产品缺陷须实测，不能机械计为 5 个已确认 bug。

| 来源 | 待核实内容 | 关闭所需证据 | 当前状态 |
| --- | --- | --- | --- |
| [#1](https://github.com/billlza/netdiag-twin/issues/1) | Overview 五卡等尺寸、圆角、文字/图标视觉居中和边缘裁切。 | macOS 支持窗口尺寸、缩放和中英文下的实际界面检查；相关布局回归。 | 五卡几何回归、中英文原生概览、窗口放大和键盘缩放/滚动已验证；发现的紧凑英文排版缺陷另列 D-09，修复后复验通过。 |
| [#2](https://github.com/billlza/netdiag-twin/issues/2) | 可替换模型、manifest、训练、fallback 披露、HIL 反馈导出。 | 真实模型读写、损坏输入显式失败、训练到诊断再到反馈导出的闭环回归。 | 本机 CLI 闭环通过；模型字节哈希不符和损坏 manifest 在 run 发布前拒绝。模型质量仅针对合成夹具，见验证记录。 |
| [#3](https://github.com/billlza/netdiag-twin/issues/3) | OTLP、原生 pcap/pcap 文件、系统计数器与 provenance。 | 各受支持采集入口的实际输入、错误分类、超时/取消/资源边界及质量证据；设备验收单列。 | OTLP 真实 loopback gRPC、pcap 文件与截断拒绝通过；D-04/D-05 已修复并复验。实际 `iface:lo0` 采集因 macOS `/dev/bpf0` 权限被拒绝，退出 1 且无结果输出，符合既有权限契约；未修改设备权限。物理采集与实验室证明属于 L-04 验收缺口，未据此声称通过。 |
| [#4](https://github.com/billlza/netdiag-twin/issues/4) | 拓扑 JSON/YAML 导入导出、校验、快照、策略 what-if。 | 往返保真、非法拓扑拒绝、策略计算和 before/after 验证；界面操作与 CLI 一致。 | 核心往返/非法输入回归、CLI 策略计算及 Pilot after-run 闭环通过；原生 GUI 导入 YAML、显示 3 节点/2 链路并导出 JSON，经独立 CLI 校验通过。 |
| [#5](https://github.com/billlza/netdiag-twin/issues/5) | DMG、签名/公证、Sparkle、GitHub Release、Homebrew。 | 同一候选的包哈希、签名/公证/安装结果；发布后校验 Release/appcast/cask 一致，并从现有正式版执行更新 smoke。 | 本机 Developer ID、Apple 公证、DMG/App Gatekeeper 及隔离安装/原生启动通过；签名密钥与旧版更新公钥一致。正式 main CI、Release/appcast/cask 发布及原安装的 Sparkle 更新仍待完成。 |
| [历史 Dependabot 失败](https://github.com/billlza/netdiag-twin/actions/runs/29423235967) | 2026-07-15、旧提交 `d6745e9a16e37b61a2d297d805f98c3111ccdf78` 的 `Run Dependabot` 失败。 | 确认更新器失败原因、是否仍影响当前依赖更新及恢复结果；旧日志不能证明当前产品运行失败。 | 旧 SDK 依赖范围阻止安全更新；登记基线 0.5.3 已为 SDK 0.32.1，本次候选保留此修复并通过新 advisory 检查。未把历史调度任务本身标为已重跑。 |

如果复核发现 bug，登记为 D 项并完成修复；如果已有实现且验收通过，保留关闭证据；
如果属于功能请求、重复项或既定支持范围，引用契约和代码说明分类理由。
待复核项未结清前，不能宣称“所有现有已知 bug 已解决”。

## 已观察到的能力与验收缺口

这些缺口也必须在本版复核，不能隐藏或伪装成成功。当前材料把它们定义为安全边界、
能力限制或证据不足，尚不能直接断言它们都是产品 bug。若实际行为违背现有承诺，
须转为 D 项修复；不得通过事后缩减承诺把已确认 bug 改名为限制。

| 编号 | 当前事实与来源 | 后续要求 |
| --- | --- | --- |
| L-01 | Windows 缺少受审查的目录持久化/发布边界，数据集写入、模型训练/重建、HIL、run 创建和历史清理受限。见[平台契约](getting-started.md#platform-mutation-contract)、[模型发布](model-bundle-publication.md)及 `crates/netdiag-platform/src/atomic_directory.rs`。 | 核对所有入口和对外承诺；如实现支持，必须提供原生 Windows 的崩溃恢复、并发、不覆盖和无副作用失败证据，不能使用空 flush 或默认成功。 |
| L-02 | Windows Python 适配器因解释器/祖先目录信任与进程控制边界尚未完成而禁用。见 `crates/netdiag-core/src/python_runtime.rs` 和 `crates/netdiag-core/src/pilot/adapter_contract/process.rs`。 | 核对支持范围和错误反馈；如实现支持，验证 DACL、身份绑定、超时、取消、进程回收和输出上限。 |
| L-03 | [tc/netem 适配器](../examples/adapters/tc-netem-lab/README.md)只输出只读计划，未提供已验证的主动 qdisc 修改与崩溃安全回滚。 | 核对用户可见能力与实际行为；主动网络修改须有明确授权和可靠回滚，不能为消除限制而直接放开。 |
| L-04 | [真实设备状态](real-device-pilot-readiness.json)为 `pending_lab_access` / `not_validated`，`real_device_release_claim_eligible=false`。 | 取得真实设备采集、after-run 验证和已审阅证据前，不声称物理设备验收通过；任何因此无法验证的具体 bug 保持未关闭。 |

## 历史审查问题的当前复核

- PR #8 的 [vendored Cargo.lock 缺失意见](https://github.com/billlza/netdiag-twin/pull/8#discussion_r3587936966)：
  本次在登记基线运行 `python3 scripts/check_patch_contract_hygiene.py`，通过
  （2 个本地补丁）；该旧失败未在当前检查中复现。
- PR #8 的 [architecture guard 路径缺失意见](https://github.com/billlza/netdiag-twin/pull/8#discussion_r3587936970)：
  本次运行 `bash scripts/check_architecture_guard.sh`，退出码 0，路径/行数和
  依赖方向检查通过；该旧失败未在当前检查中复现。

保留这两项的回归检查；上述结果不证明其他问题已经解决。

## 0.5.4 发布验收

1. 完成已知问题来源复核，所有已确认 bug 的未关闭数量为 **0**，所有历史待复核项
   均有可审查结论。优先级低、难复现、外部阻塞或缺少设备均不能自动关闭 bug。
2. 每个修复都有相关回归证据；尽可能保存旧实现失败、新实现通过的结果。
   无法执行旧版时写明原因并提供等效的可达路径/输入证据，不虚称 red/green。
3. 按[质量门禁](quality-gates.md)执行 strict gate，并完成受影响的原生平台、
   采集到诊断到报告/证据导出、持久化/恢复和 GUI 实际交互验证。
   不降低断言、覆盖率或 warning 要求；不能用 mock 代替受影响的真实边界。
4. 按[发布流程](release-process.md)升级候选版本到 `0.5.4`，验证该精确 main
   提交的 CI 和同一候选的签名、公证、安装证据后，才能推送 `v0.5.4` tag。
5. 发布后核对 Release、Sparkle appcast 和 Homebrew 指向同一版本和制品，完成
   旧版更新 smoke；这些结果完成前不宣布本次发布交付完成。
6. Release notes 根据实际关闭的 D 项及验收结果编写，保留能力边界和未完成的
   真实设备验收状态。不得直接把本计划改写成“所有 bug 已修复”的公告。

执行阶段已完成依赖迁移与上述缺陷修复。`strict-06-macos-isolation.log` 完整通过，
后续真实 CLI 复核进一步确认 D-05 的 pcap/proxy 问题，修复后的
`strict-07-provenance.log` 的测试通过，但新增披露使单函数超过既有复杂度上限。
已将两类采集器共用的缺失事件披露提取为共享函数，
`strict-08-provenance-shared.log` 完整通过，退出码 0。
随后旧数据及 GUI 复验发现 D-06 至 D-10，本机修复、相关回归和原生复验均已完成。
随后补齐保留原模型的显式迁移并修复浮点往返问题。最新
`strict-13-model-cli-module.log` 完整通过：1,173 Rust 测试、206 Python 检查。
Developer ID 签名、公证、Gatekeeper 与隔离安装已通过；正式发布及旧版更新仍待完成。原始记录保存于
`target/verification/v0.5.4-20260912/` 与 `target/verification/v0.5.4-20260913/`，失败记录保留。
命令、结果与验收边界见[验证记录](v0.5.4-validation.md)。

## 执行环境与发布阻塞（2026-09-13）

- 当前源目录及 Desktop 为 0775；strict 的 Pilot 正向测试被既有执行目录信任检查拒绝。
  保留了这次失败与源目录权限，未放宽验证。改用 `/Users/bill/netdiag-054-validation.mw3r0qyq`
  的私有隔离 worktree 验证相同候选；补丁 SHA-256 和快照来源存于
  `target/verification/v0.5.4-20260912/trusted-validation-root.json`。
- 四个发布环境均已配置为仅允许 `v*` tag、禁止管理员绕过，不要求独立审核。
  main ruleset 要求 PR 和五项精确 CI；另外两个 ruleset 限定标签创建者并禁止修改/删除
  发布标签。环境级 Secrets/Variables 已配置；旧 Homebrew/Sparkle 凭据通过
  [一次性加密迁移任务](https://github.com/billlza/netdiag-twin/actions/runs/34741162454)
  保留原值，任务完成后已禁用。仓库级旧副本将在 main 切换到新流程后、tag 前清退。
  Immutable Releases 已启用，发布前仍须重新核对。
- `/Applications` 中仍为 0.5.0，并已备份。0.5.4 release 构建 `202609131406` 已通过
  Developer ID 签名、公证、DMG/App Gatekeeper、隔离安装和中英文原生复验；
  DMG SHA-256 为 `07a80bff61431a62420b6dcc045e03728aaa2bfb4fc11fabb2796601e81e111c`。
  GitHub 正式制品、原安装更新及更新后原始数据验收仍待完成。
- 经用户授权，53 个旧数据目录已从 0775 收紧至 0755；461 个文件的内容、身份、
  权限和所有者均未改变，备份与逐项日志已核验。随后发现模型目录需要 0700、
  历史数据还需要显式根迁移及模型格式迁移；当前先在独立数据副本上演练，
  未对原始数据执行这些新增步骤。第三份副本的保留模型迁移、幂等与诊断已通过；
  原安装更新后再迁移原始数据，以免旧版 App 向新模型布局写入旧文件。
  Desktop 和源码目录权限未修改。
- macOS 并行启动 nextest 时，两个不启动子进程的测试曾被报告输出管道泄漏。
  50 次隔离执行和 5 轮串行对照通过，5 轮并行邻近测试中复现一次；上游 Rust/nextest
  的管道创建路径支持进程启动时继承的归因。macOS 改为串行测试进程，保留测试内部并发、
  200 ms 泄漏失败标准和全部断言，无自动重试。完整门禁随后通过。
- [依赖升级与兼容约束](v0.5.4-dependencies.md)记录了版本来源、无法直接跨越的上游约束
  和已通过的依赖检查。
- 候选代码位于[PR #9](https://github.com/billlza/netdiag-twin/pull/9)。`9bc32e5` 五项 CI
  已通过；`2a1071f` 的四项通过，macOS strict 发现 CLI 文件 1,361 行超过原有 1,360
  行上限。已把模型输出移入模型模块，main 降为 1,331 行且本机 strict-13 全部通过。
  最终提交及合并后的精确 main SHA 都必须重新通过 CI，不能借用旧 SHA 的结果。

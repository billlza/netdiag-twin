schema: netdiag-lab-scenario/v1
id: ood-routing-loop-001
name: Unknown routing loop benchmark

data_sources:
  - name: ood-routing-loop-trace
    role: primary
    kind: trace-file
    endpoint: ../../data/samples/ood_routing_loop.csv

acceptance:
  min_ml_probability: 0.0
  allowed_diagnosis_statuses:
    - uncertain
    - out_of_distribution
  require_rule_ml_agreement: false
  require_what_if_improvement: false

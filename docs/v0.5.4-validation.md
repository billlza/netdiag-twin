# 0.5.4 候选验证记录

日期：2026-09-13。旧数据兼容与原生界面修复、最新本机完整门禁均已通过；发布验收不完整。

源分支为 `lza/v0.5.4-known-bugs-and-dependencies`，基于 main
`968e9bb051311655920234c29e961cfdac571bac`。上一轮 strict 与原生采集器验证绑定源提交
`6584025a93346fcf425366e4070596263aa02aef`，随后 `315376b` 的五项 CI 通过。
本轮在该提交上修复 D-06 至 D-10，并按项目决定取消强制独立发布审核。
本机验证在私有 worktree
`/Users/bill/netdiag-054-validation.mw3r0qyq` 中进行。上一轮来源关系保存在
`target/verification/v0.5.4-20260912/strict-08-source-receipt.json`；本轮
`candidate-source-before-strict-11.json` 对源目录与验证目录的 958 个文件逐一比对 SHA-256。
后续文档记录独立更新，最终提交仍须原生 CI 验证。

## 本轮验证

本轮证据位于隔离 worktree 的 `target/verification/v0.5.4-20260913/`。

| 检查 | 实际结果与证据 |
| --- | --- |
| 已授权目录权限修复 | 53 目录从 0775 改为 0755；461 文件的内容、身份、权限、所有者在修复前后均一致。`permission-repair-completed.json` 绑定逐项日志和备份 SHA-256 `14562aae15cb6cb65b23deed8e5a8a7926ad4d079f7803d6ca2b6919cd8cbcac`。 |
| 历史统计与路径 red/green | 缺失 P50/P99 的读取和迁移在旧实现失败；绝对路径迁移旧实现失败。分别记录于 `legacy-window-red.log`、`legacy-report-migration-red.log`、`legacy-absolute-path-red.log`。修复后 40 项统计/图表/迁移回归及 57 项存储边界回归通过。 |
| 真实旧数据的副本演练 | 第一份未重定位副本的失败保留。第二份副本仅在复制准备阶段重定位 47 个索引/manifest 的绝对路径，并把副本模型目录设为 0700；变换单列于 `copy-relocation-receipt.json`。随后产品迁移保持 460 个既有文件字节不变，只增加归属标记，CLI 读取 50 条历史记录及旧报告证据成功。未把副本路径重定位描述为产品原地迁移。 |
| 原生模型和拓扑操作 | 副本上通过 GUI 确认重建模型，旧模型字节保存在两代模型中的一代，新仿真诊断成功。GUI 导入 `line.yaml` 显示 3 节点/2 链路；导出 JSON 经独立 CLI 校验为 valid。见副本的 `model-rebuild-receipt.json` 和 `gui-topology-validation.json`。 |
| 英文紧凑布局 red/green | `compact-ui-red-03.log` 使用应用字体复现标题/置信度重叠、状态标签/值重叠和字距异常；修复后相应测试通过。`gui-regressions-green-01.log` 中 53 项设置、表单和渲染测试通过。 |
| 原生 GUI 复验 | 调试构建 `202609131121` 通过中英文概览、键盘放大与滚动检查；空 API 不再误报 Keychain 错误。退出旧进程后重新启动，显式数据目录保留并完成自动诊断；报告页可打开旧运行的证据。见 `gui-restart-and-layout-receipt.json`。该构建仅为 ad-hoc 调试包。 |
| strict-09 | 1,160 Rust 测试、206 Python 检查及完整门禁通过；核心/CLI/平台覆盖率 82.55%（31,922/38,672），critical 96.41%，应用安全 86.07%。这是后续 GUI 修复之前的快照。 |
| strict-10 | 1,167 Rust 测试通过；Python 检查发现新增表单函数使 `connector_auth.rs` 超过 70 行上限（74 行），整轮失败。已拆分表单子模块并保持原门槛，最终完整复验另行记录。 |
| strict-11 最终完整检查 | `strict-11-gui-scope-module.log` 退出 0：1,167 Rust 测试、206 Python 检查、依赖、架构、复杂度、适配器、性能、Pilot/calibration/model-gate 均通过。覆盖率为核心/CLI/平台 82.54%（31,921/38,672）、critical 96.41%（4,510/4,678）、应用安全 86.07%（1,402/1,629）。`strict-11-final-receipt.json` 绑定源码快照及日志哈希。 |
| 最终包复验与设置恢复 | 模块拆分后重新构建调试包 `202609131134`，验证 ad-hoc 签名，启动后保持所选目录、完成 80 行诊断并正确显示未配置 API 状态。结束后停止测试进程并恢复原设置；461 个原文件的内容及权限均与权限修复前清单一致。设置文件因原子恢复而更换 inode，原始模型及归属标记没有迁移。见 `debug-package-receipt-05.json`、`gui-final-and-restoration-receipt.json`。 |
| 发布审核策略 | 工作流不再强制 required reviewers/prevent self-review，校验脚本与发布文档同步。206 项脚本检查在 `release-policy-quality-01.log` 通过；GitHub 唯一现有环境没有 required-reviewer 规则。其余自动质量、部署范围与制品验证要求保留。 |

## 上一轮实际检查

以下路径相对于隔离 worktree；源目录保留早期失败与来源资料，没有覆盖失败尝试。

| 检查 | 实际结果与证据 |
| --- | --- |
| `scripts/check_rust_quality.sh strict`，Rust 1.98.1，`RUSTFLAGS=-D warnings` | 最终 `strict-08-provenance-shared.log` 退出 0：1,149 个 Rust 测试和 206 个 Python 检查通过；依赖、复杂度、架构、适配器、性能、Pilot/calibration/model-gate 均通过。06 轮的通过和 07 轮的单函数复杂度失败仍保留；共享事件披露修正后的结果以 08 轮为准。 |
| 覆盖率 | 08 轮的核心/CLI/平台覆盖率 82.54%（31,914/38,663），release-critical 96.41%（4,510/4,678）；应用安全聚合覆盖率 86.07%（1,402/1,629），逐文件下限通过。没有修改覆盖率标准。 |
| Overview red/green | 宽度旧实现失败于 `overview-width-red.log`；滚动旧实现失败于 `overview-scroll-red-03.log`；5 项修复后回归通过于 `overview-layout-green-02.log`。这是几何/滚动测试，不是原生截图验收。 |
| 系统计数器解析 red/green | `netstat-format-red-02.log` 证明别名行与空 Address 缺陷；`netstat-format-green.log` 中 17 项通过。`all` 和 `lo0` 的真实 CLI 采集成功，最终结果为 `system-counters-{all,lo0}-final-02.json`。 |
| 指标质量 red/green | `netstat-provenance-red.log`、`pcap-provenance-red.log`、`counter-estimate-red.log` 保留旧质量标注失败。最终 strict 通过，真实系统计数器与合成 pcap 的 CLI 均为 1 measured / 1 estimated / 8 fallback；见 `native-collector-final-receipt.json`。 |
| `cargo test --locked -p netdiag-app --lib website_probe_can_collect_default_public_targets -- --ignored --nocapture` | 主动公网 smoke 单独通过；默认测试仍显式忽略这项外网检查。未修改代理、DNS、路由或 TUN。 |
| CLI 模型到反馈闭环 | `cli-acceptance-01/receipt.json` 记录独立进程命令：初始化私有根、JSONL 训练、CSV 诊断、合成夹具 HIL 记录、反馈导出/校验、拓扑与策略验证、what-if、证据导出；输出 1 行反馈、含 15 文件的 ZIP，ZIP 完整性检查通过。 |
| CLI 损坏输入 | 截断 pcap、模型字节哈希不符、非法 manifest 均显式失败；后两者没有发布 run。记录在同一 CLI receipt 及 stderr 中。 |
| OTLP 边界 | `generated_grpc_client_exports_and_snapshots_a_complete_observation` 使用生成的 gRPC 客户端和真实 loopback socket；完整观测、连接释放及 graceful shutdown 断言通过，部分 HTTP/2 连接的强制有界回收另有回归。 |
| pcap 文件边界 | 独立 CLI 读取含合法 Ethernet/IPv4/TCP 帧的合成 classic-pcap，截断版本失败；现有加载/取消/期限/符号链接/容量测试通过。这不是物理网卡采集证据。 |
| 依赖、脚本和工作流 | 最新 advisory 数据库结果为 advisories/bans/licenses/sources 全部通过，无 advisory waiver。Shellcheck、Actionlint 与 Python 门禁已通过；最终文档/提交另行复核。 |

CLI 实验保留了两次验证输入修正：大写 `Congestion` 被标签解析器拒绝后，使用合法的
小写值完成闭环；给 manifest 添加合法空白仍可加载，因为格式不对 manifest 来源作认证，
报告记录本次读取的确切哈希。没有将合法空白接受误报为产品缺陷；真正的非法 JSON
与模型哈希不符通过独立目录验证为失败。所有样本/HIL 记录均为隔离合成夹具，未执行网络策略。

## 尚未具备的验收证据

- 原始应用数据还需要模型目录 0700、显式归属迁移和旧模型格式迁移。副本上的迁移、
  中英文概览、缩放滚动、历史证据和 GUI 拓扑导入导出已验证；这些结果尚未应用到原始模型。
- 已构建的调试包仅通过 ad-hoc `codesign --verify --deep --strict`，不是 Developer ID
  签名、公证、正式安装或 Sparkle 更新验收。最新调试构建号为 `202609131134`，
  包内二进制 SHA-256 为 `b2ab3bfc1d862c11d114c4fc8e0e6c753170a776c84807f621a1e8478f4d728e`；
  `debug-package-receipt-05.json` 绑定源码快照、打包前/后哈希及签名结果。只读检查确认本机
  存在有效 Developer ID 身份；本次未使用该私钥签名，也未指定公证 profile。
- 远端缺少 `release-signing`、`release-publication`、`release-homebrew` 保护环境及
  release tag ruleset。独立审核已改为非必需条件。Immutable Releases 已启用。
  未创建发布 tag 或降低发布校验。
- 原生 Windows/Linux CI 已在 `6584025` 通过；同轮 macOS adapter-schema 因 Rustup
  组件文件冲突失败，已用任务独立安装目录修正。同版本的全新目录安装与自动补齐已本机通过。
  最新精确提交的原生 CI 以[PR #9 检查](https://github.com/billlza/netdiag-twin/pull/9/checks)为准。
  Windows 的持久化发布与 Python 适配器限制保留，tc/netem 保持只读；真实设备状态仍为
  `pending_lab_access` / `not_validated`。

已通过的检查仅支持相应范围；上述缺口未补齐前，不能宣布“所有已知 bug 已关闭”或
“0.5.4 已发布”。完整问题范围见[版本台账](v0.5.4-plan.md)。

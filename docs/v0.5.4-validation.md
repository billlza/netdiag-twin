# 0.5.4 候选验证记录

日期：2026-09-13。已确认缺陷修复、本机完整门禁、Developer ID、公证、Gatekeeper、
隔离安装及原生复验已通过；正式发布、原安装更新和原始数据迁移仍待完成。

源分支为 `lza/v0.5.4-known-bugs-and-dependencies`，基于 main
`968e9bb051311655920234c29e961cfdac571bac`。上一轮 strict 与原生采集器验证绑定源提交
`6584025a93346fcf425366e4070596263aa02aef`，随后 `315376b` 的五项 CI 通过。
本轮在该提交上修复 D-06 至 D-10，并按项目决定取消强制独立发布审核。
本机验证在私有 worktree
`/Users/bill/netdiag-054-validation.mw3r0qyq` 中进行。上一轮来源关系保存在
`target/verification/v0.5.4-20260912/strict-08-source-receipt.json`；本轮
`candidate-source-before-strict-11.json` 对源目录与验证目录的 958 个文件逐一比对 SHA-256。
后续文档记录独立更新，最终提交仍须原生 CI 验证。

## 发布候选复验

以下新证据仍位于私有 worktree 的 `target/verification/v0.5.4-20260913/`。
`candidate-source-before-strict-13.json` 比对 962 个源文件；后续仅发布记录文档独立更新，
最终 commit 的关系由候选 receipt 及新 CI 记录绑定。

| 检查 | 实际结果与证据 |
| --- | --- |
| 保留旧模型的显式迁移 | `netdiag model migrate` 复用原有串行、原子、持久化发布路径，保留选中模型的字节和已有训练元数据值。21 项相关核心测试及 38 项 CLI 测试通过，Windows 新增迁移在模型和锁文件 I/O 前拒绝的回归。 |
| 浮点往返 red/green | `model-migration-regressions-02.log` 中精确相等断言失败：一个阈值从 `40.100254306676476` 变成 `40.10025430667648`。启用 serde_json `float_roundtrip` 后，同一断言及其余 21 项相关测试通过于 `model-migration-regressions-03.log`。 |
| 第三份真实旧数据副本 | `legacy-migration-rehearsal-03/` 从原始 460 文件复制；仅复制准备阶段重定位 47 个元数据文件并设副本模型目录为 0700。产品根迁移保留原文件，模型迁移保持权重字节和已有训练元数据值；第二次迁移不改文件，50 条历史可读，后续诊断成功。原始数据未变。 |
| 副本验证断言修正 | 原断言把旧 manifest 中缺省的 `label_distribution` 与 v2 序列化的 `{}` 视为差异。根据 `ModelManifest` 既有 `serde(default)` 契约，验证显式记录这一个空映射默认值，保持其余字段精确相等。没有补造标签计数；失败保留于 `metadata-verification-failure-01.json`，完成证据为 `migration-completed-receipt.json`。 |
| strict-12 与中间 CI 失败 | 本机 1,173 Rust 测试、206 Python 检查通过，随后 CLI main 的 1,361 行超过原有 1,360 上限。`2a1071f` 的 [CI](https://github.com/billlza/netdiag-twin/actions/runs/34741241909) 四项通过，macOS strict 同因失败。模型输出已移入模型模块，main 降为 1,331 行；上限保持不变。 |
| strict-13 最终本机门禁 | `strict-13-model-cli-module.log` 退出 0：1,173 Rust 测试、206 Python 检查、全部代码/依赖/架构/适配器/性能/Pilot 门禁通过。核心/CLI/平台覆盖率 82.54%（31,958/38,718），critical 96.41%（4,510/4,678），应用安全 86.07%（1,402/1,629）。公网 smoke 仍为显式忽略项，其早前单独运行结果保留。 |
| 签名和公证 | release 构建 `202609131406` 使用既有 Developer ID；Apple submission `4f944dcd-a518-42b2-b34f-607e7bd0e3c0` 为 Accepted。DMG SHA-256 `07a80bff61431a62420b6dcc045e03728aaa2bfb4fc11fabb2796601e81e111c`，包内主程序 SHA-256 `ce026f10571e9dbee9331968edb2b324dd44a6c7bc570acaf769d28e46c3d7f6`。见 `signed-release-package-01-receipt.json`。 |
| DMG、Gatekeeper 和隔离安装 | 镜像完整性、DMG 签名、stapler、DMG/App Gatekeeper 以及深度严格 App 签名检查全部退出 0；从只读挂载复制到独立安装目录，主程序字节与候选一致。见 `isolated-installation-01-receipt.json`。这不是 `/Applications` 原安装的更新。 |
| 签名包原生复验 | 隔离安装的真实进程显示 0.5.4，中英文概览、五卡、诊断/状态排版正常；80 行自动仿真使用保留的旧模型，运行记录中的模型哈希一致。Reports 实际显示旧运行 `476b6a68-8ad6-4acf-8f29-b05e79f42ec1` 的摘要与制品；API 空地址显示未配置。退出后恢复设置，461 个原文件内容和权限与原清单一致。见 `signed-release-native-gui-01.json`、`signed-release-gui-restoration-receipt.json`。截图在任务中观察，未导出独立图像文件。 |
| 原安装备份 | `/Applications/NetDiag Twin.app` 仍为 0.5.0 build 25，签名验证通过。`installed-0.5.0-before-update.zip` SHA-256 为 `a5e39f4313d544ba45c2547be0cf02add210902d7ba496508f90ea933d29bd53`。 |
| 真实接口权限边界 | 实际执行 loopback `native-pcap`，因 `/dev/bpf0: Permission denied` 退出 1，stdout 为 0 字节。未改变 BPF、代理、DNS、路由或 TUN 权限/配置；该结果不是成功采集或物理设备验收。 |
| 发布环境与凭据 | 四环境均只允许 `v*` 标签且禁止管理员绕过，不要求独立审核；三个 ruleset 约束 main PR/五项 CI、标签创建者及标签不可修改/删除。环境级设置已核对。原 Homebrew/Sparkle Secret 通过 [GitHub 内一次性任务](https://github.com/billlza/netdiag-twin/actions/runs/34741162454) 加密到目标环境，认证和 Sparkle 公钥连续性检查通过；明文未进入本机或日志，任务已禁用。main 合并后 10 个仓库级旧副本已清退，并再次核对环境级名称和部署规则；见 `repository-secret-retirement-completed.json`。 |
| 最终产品提交 CI | `7e9ddde` 的 [CI 34743185148](https://github.com/billlza/netdiag-twin/actions/runs/34743185148) 五项全部通过，PR #9 已合并为 main `def1d5c`，文件树一致。该 main 的 [CI 34744706717](https://github.com/billlza/netdiag-twin/actions/runs/34744706717) 四项成功，adapter-schema 失败；原始失败日志为 `main-def1d5c-adapter-failure-01.log`，没有用 PR 结果替代 main 验证。 |
| macOS Rust 组件安装 | main 失败发生在首次 Cargo 调用自动补装时，报 `bin/cargo-clippy` 冲突。现对 adapter-schema 和 macos_compile 显式安装完整组件，回归覆盖缺失、注释和安装顺序；旧工作流在新增 guard 下失败。`rust-component-setup-comparison-02/comparison.json` 使用全新私有 Rustup/Cargo 目录证实：minimal 原配置在首次 Cargo 调用补装三个组件；新配置提前装齐、Cargo 退出 0、组件集合不变且无补装日志。本机未复现文件冲突，因此未把 Rustup 内部冲突机制称为已证明。207 项 Python 回归、release hygiene、Actionlint 和 diff 检查通过，日志为 `mac-rust-components-quality-01.log`。 |

## 本轮验证

发布前新增 Homebrew 审核回归：旧步骤只修改本地 tap 的工作区，后续 Git 克隆仍取得
已提交的旧 cask。`homebrew-audit-clone-red-01.log` 在真实临时 Git 仓库中复现
0.5.4 候选被克隆为 0.5.2；修复改为先提交审核候选，再于实际审核前后比较字节。
`homebrew-audit-clone-green-01.log` 的同一克隆回归及候选绑定 guard 通过，重复准备
没有增加提交。此回归验证 Git 克隆边界，正式 Homebrew audit 结果另由发布工作流提供。
两项工作流修复合并后，`release-workflow-quality-02.log` 记录 209 项 Python 回归通过；
release hygiene、Actionlint 和 diff 检查也通过。835 个产品源码/依赖文件与 strict-13
验证目录一致，产品代码没有因本轮工作流修复而改变。

`defc00c` 的 [CI 34746772215](https://github.com/billlza/netdiag-twin/actions/runs/34746772215)
五项全部通过；原始 macOS 日志记录 1,173 Rust 测试及 209 Python 回归通过。
合并前复核[自动审查意见](https://github.com/billlza/netdiag-twin/pull/10#discussion_r3999150733)
发现 D-15：组件文字检查没有验证 YAML 层级和步骤执行条件。12 个反例在旧 guard 下
被误接受，修复改为核对保留缩进的完整 action 结构，全部反例随后拒绝。第一次新实现
因固定 action 常量已含 `uses:` 而重复拼接，合法配置回归失败于
`mac-toolchain-structure-green-01.log`；修正拼接后的 `green-02` 通过。失败记录均保留，
`release-workflow-quality-03.log` 的 209 项 Python 回归全部通过；release hygiene、
Actionlint 和 diff 检查也通过。最终提交和 main CI 仍须重新验证。

本轮证据位于隔离 worktree 的 `target/verification/v0.5.4-20260913/`。

| 检查 | 实际结果与证据 |
| --- | --- |
| 已授权目录权限修复 | 53 目录从 0775 改为 0755；461 文件的内容、身份、权限、所有者在修复前后均一致。`permission-repair-completed.json` 绑定逐项日志和备份 SHA-256 `14562aae15cb6cb65b23deed8e5a8a7926ad4d079f7803d6ca2b6919cd8cbcac`。 |
| 历史统计与路径 red/green | 缺失 P50/P99 的读取和迁移在旧实现失败；绝对路径迁移旧实现失败。分别记录于 `legacy-window-red.log`、`legacy-report-migration-red.log`、`legacy-absolute-path-red.log`。修复后 40 项统计/图表/迁移回归及 57 项存储边界回归通过。 |
| 真实旧数据的副本演练 | 第一份未重定位副本的失败保留。第二份副本仅在复制准备阶段重定位 47 个索引/manifest 的绝对路径，并把副本模型目录设为 0700；变换单列于 `copy-relocation-receipt.json`。随后产品迁移保持 460 个既有文件字节不变，只增加归属标记，CLI 读取 50 条历史记录及旧报告证据成功。未把副本路径重定位描述为产品原地迁移。 |
| 原生模型和拓扑操作 | 副本上通过 GUI 确认重建模型，旧模型字节保存在两代模型中的一代，新仿真诊断成功。GUI 导入 `line.yaml` 显示 3 节点/2 链路；导出 JSON 经独立 CLI 校验为 valid。见副本的 `model-rebuild-receipt.json` 和 `gui-topology-validation.json`。 |
| 英文紧凑布局 red/green | `compact-ui-red-03.log` 使用应用字体复现标题/置信度重叠、状态标签/值重叠和字距异常；修复后相应测试通过。`gui-regressions-green-01.log` 中 53 项设置、表单和渲染测试通过。 |
| 原生 GUI 复验 | 调试构建 `202609131121` 通过中英文概览、键盘放大与滚动检查；空 API 不再误报 Keychain 错误。退出旧进程后重新启动，显式数据目录保留并完成自动诊断；报告页可打开旧运行的证据。见 `gui-restart-and-layout-receipt.json`。该构建仅为 ad-hoc 调试包。 |
| strict-09 | 1,160 Rust 测试、206 Python 检查及完整门禁通过；核心/CLI/平台覆盖率 82.55%（31,922/38,672），critical 96.41%，应用安全 86.07%。这是后续 GUI 修复之前的快照。 |
| strict-10 | 1,167 Rust 测试通过；Python 检查发现新增表单函数使 `connector_auth.rs` 超过 70 行上限（74 行），整轮失败。已拆分表单子模块并保持原门槛，最终完整复验另行记录。 |
| strict-11 最终完整检查 | `strict-11-gui-scope-module.log` 退出 0：1,167 Rust 测试、206 Python 检查、依赖、架构、复杂度、适配器、性能、Pilot/calibration/model-gate 均通过。覆盖率为核心/CLI/平台 82.54%（31,921/38,672）、critical 96.41%（4,510/4,678）、应用安全 86.07%（1,402/1,629）。`strict-11-final-receipt.json` 绑定源码快照及日志哈希。 |
| 最终包复验与设置恢复 | 模块拆分后重新构建调试包 `202609131134`，验证 ad-hoc 签名，启动后保持所选目录、完成 80 行诊断并正确显示未配置 API 状态。结束后停止测试进程并恢复原设置；461 个原文件的内容及权限均与权限修复前清单一致。设置文件因原子恢复而更换 inode，原始模型及归属标记没有迁移。见 `debug-package-receipt-05.json`、`gui-final-and-restoration-receipt.json`。 |
| 发布审核策略 | 工作流不再强制 required reviewers/prevent self-review，校验脚本与发布文档同步。206 项脚本检查在 `release-policy-quality-01.log` 通过；GitHub 唯一现有环境没有 required-reviewer 规则。其余自动质量、部署范围与制品验证要求保留。 |

## 上一轮实际检查

以下路径相对于隔离 worktree；源目录保留早期失败与来源资料，没有覆盖失败尝试。

| 检查 | 实际结果与证据 |
| --- | --- |
| `scripts/check_rust_quality.sh strict`，Rust 1.98.1，`RUSTFLAGS=-D warnings` | 最终 `strict-08-provenance-shared.log` 退出 0：1,149 个 Rust 测试和 206 个 Python 检查通过；依赖、复杂度、架构、适配器、性能、Pilot/calibration/model-gate 均通过。06 轮的通过和 07 轮的单函数复杂度失败仍保留；共享事件披露修正后的结果以 08 轮为准。 |
| 覆盖率 | 08 轮的核心/CLI/平台覆盖率 82.54%（31,914/38,663），release-critical 96.41%（4,510/4,678）；应用安全聚合覆盖率 86.07%（1,402/1,629），逐文件下限通过。没有修改覆盖率标准。 |
| Overview red/green | 宽度旧实现失败于 `overview-width-red.log`；滚动旧实现失败于 `overview-scroll-red-03.log`；5 项修复后回归通过于 `overview-layout-green-02.log`。这是几何/滚动测试，不是原生截图验收。 |
| 系统计数器解析 red/green | `netstat-format-red-02.log` 证明别名行与空 Address 缺陷；`netstat-format-green.log` 中 17 项通过。`all` 和 `lo0` 的真实 CLI 采集成功，最终结果为 `system-counters-{all,lo0}-final-02.json`。 |
| 指标质量 red/green | `netstat-provenance-red.log`、`pcap-provenance-red.log`、`counter-estimate-red.log` 保留旧质量标注失败。最终 strict 通过，真实系统计数器与合成 pcap 的 CLI 均为 1 measured / 1 estimated / 8 fallback；见 `native-collector-final-receipt.json`。 |
| `cargo test --locked -p netdiag-app --lib website_probe_can_collect_default_public_targets -- --ignored --nocapture` | 主动公网 smoke 单独通过；默认测试仍显式忽略这项外网检查。未修改代理、DNS、路由或 TUN。 |
| CLI 模型到反馈闭环 | `cli-acceptance-01/receipt.json` 记录独立进程命令：初始化私有根、JSONL 训练、CSV 诊断、合成夹具 HIL 记录、反馈导出/校验、拓扑与策略验证、what-if、证据导出；输出 1 行反馈、含 15 文件的 ZIP，ZIP 完整性检查通过。 |
| CLI 损坏输入 | 截断 pcap、模型字节哈希不符、非法 manifest 均显式失败；后两者没有发布 run。记录在同一 CLI receipt 及 stderr 中。 |
| OTLP 边界 | `generated_grpc_client_exports_and_snapshots_a_complete_observation` 使用生成的 gRPC 客户端和真实 loopback socket；完整观测、连接释放及 graceful shutdown 断言通过，部分 HTTP/2 连接的强制有界回收另有回归。 |
| pcap 文件边界 | 独立 CLI 读取含合法 Ethernet/IPv4/TCP 帧的合成 classic-pcap，截断版本失败；现有加载/取消/期限/符号链接/容量测试通过。这不是物理网卡采集证据。 |
| 依赖、脚本和工作流 | 最新 advisory 数据库结果为 advisories/bans/licenses/sources 全部通过，无 advisory waiver。Shellcheck、Actionlint 与 Python 门禁已通过；最终文档/提交另行复核。 |

CLI 实验保留了两次验证输入修正：大写 `Congestion` 被标签解析器拒绝后，使用合法的
小写值完成闭环；给 manifest 添加合法空白仍可加载，因为格式不对 manifest 来源作认证，
报告记录本次读取的确切哈希。没有将合法空白接受误报为产品缺陷；真正的非法 JSON
与模型哈希不符通过独立目录验证为失败。所有样本/HIL 记录均为隔离合成夹具，未执行网络策略。

## 正式发布仍需完成的步骤

- PR #9 已合并；[PR #10](https://github.com/billlza/netdiag-twin/pull/10) 处理 D-13 至 D-15
  的工作流修复，并等待修复后精确 main SHA 的 push CI。
  此前的 PR 或旧提交结果不能替代它。
- 仓库级旧凭据清退已完成；再次核对环境和 Immutable Releases，再推送 annotated `v0.5.4`。
  验证 GitHub 正式制品、Release attestation、Pages appcast 与 Homebrew cask 一致。
- 从 `/Applications` 中的 0.5.0 执行 Sparkle 更新。更新前已保留安装备份；更新后重新
  备份原始数据，完成模型目录 0700、显式根迁移和保留模型迁移，再验证实际运行与历史。
  先更新旧 App，再迁移原数据，避免旧版向新模型布局写回旧文件。
- Windows 的持久化发布与 Python 适配器限制保留，tc/netem 保持只读；真实设备状态仍为
  `pending_lab_access` / `not_validated`，取得真实实验室证据前不得声称通过相关验收。

已确认缺陷的本机关闭证据见[版本台账](v0.5.4-plan.md)。上述发布步骤完成前，不能宣布
正式发布交付完成，也不能把本机候选包称为 GitHub workflow 的正式制品。

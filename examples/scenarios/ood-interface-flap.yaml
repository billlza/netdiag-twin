schema: netdiag-lab-scenario/v1
id: ood-interface-flap-001
name: Unknown interface flap benchmark

data_sources:
  - name: ood-interface-flap-trace
    role: primary
    kind: trace-file
    endpoint: ../../data/samples/ood_interface_flap.csv

acceptance:
  min_ml_probability: 0.0
  allowed_diagnosis_statuses:
    - uncertain
    - out_of_distribution
  require_rule_ml_agreement: false
  require_what_if_improvement: false

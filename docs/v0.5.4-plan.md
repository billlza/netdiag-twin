# NetDiag Twin 0.5.4 版本计划与缺陷台账

登记日期：2026-09-12。目标版本：**0.5.4**。

用户要求：**下个版本解决所有现有已知 bug，记作 0.5.4。**

追加要求（2026-09-12 至 2026-09-13）：依赖升级至最新稳定版，处理必要的接口迁移；上游固定的
兼容约束须提供依赖链证据，安全补丁须满足上游替代条件后才可移除。

版本定位为缺陷修复与回归验证。所有已确认缺陷均须修复并验证，严重级别只决定
处理顺序，不作为延期或豁免依据；修复期间新发现的缺陷和回归同样纳入本版。
当前状态为**执行中、复核未完成、不可发布**，本文不是已完成的修复公告。

## 登记基线

- 登记时工作区版本为 `0.5.3`；执行阶段已将候选包版本升级到 `0.5.4`，
  分支为 `lza/v0.5.4-known-bugs-and-dependencies`，尚未发布。
- 检查的本地提交：`74b1bd11a8b7b62381344e9aae1b5baa707cce4f`
  （`release/v0.5.3`），与远端 `main` 提交
  `968e9bb051311655920234c29e961cfdac571bac` 的文件树无差异。
- 登记时 GitHub 最新正式发行版仍为
  [v0.5.2](https://github.com/billlza/netdiag-twin/releases/tag/v0.5.2)。
- 上述 `main` 的 [CI 运行](https://github.com/billlza/netdiag-twin/actions/runs/29580409311)
  于 2026-07-17 创建并已成功；它只证明该提交的历史 CI 结果，不能充当 0.5.4
  候选、安装、更新或真实设备验收。

本次初始盘点读取了仓库文档、全部 5 条开放 issue、最近合并的 PR #8 审查意见和
近期 main 工作流记录。它不是完整代码审计或端到端复测。下列待复核项须逐项给出
结论，不能把“没有复现”“已有实现”或“issue 很旧”直接当成已修复。

## 已确认缺陷

| 编号 | 问题与证据 | 状态与关闭证据 |
| --- | --- | --- |
| D-01 | [短报告](short_report.md)仍称交付物为 Streamlit 应用，并引用不存在的 `setup_env.sh`；实际入口是 `crates/netdiag-app/src/main.rs` 的 `eframe::run_native`。 | 本次已修正文档：改为 Rust 桌面端与 CLI、现有 Cargo 清单和质量门禁入口；示例数值标明为演示而非实测。按文件与入口核对关闭。 |
| D-02 | Overview 五卡宽度在非整除空间下不同：旧实现把像素余量全部交给最后一张。`layout.rs` 新增测试在宽度 772 时证明旧实现失败。 | 已修复并通过 5 项布局回归；原始证据：`overview-width-red.log`、`overview-layout-green-02.log`。原生界面验收仍待完成。 |
| D-03 | 小窗口 Overview 滚动状态变化后仍按外层窗口坐标绘制，内容不随滚动移动。 | 已改用滚动子 UI 坐标和可用宽度；`overview-scroll-red-03.log` 为旧行为失败，`overview-layout-green-02.log` 为修复后通过。 |
| D-04 | 真实 macOS `system-counters` 的 `all`/`lo0` 采集失败；解析器混入 IPv4/IPv6 地址别名行，并在链路地址为空时错列读取计数。旧回归把 bytes=3000 错读为 20。 | 已修正为读取链路总计行并处理空 Address 列；17 项计数器回归通过，真正缺失/损坏/溢出的接口计数仍拒绝。补充真实 CLI 与最终 strict 复验。 |
| D-05 | 系统计数器和 pcap 把未采集的超时、重试、DNS/TLS 失败事件标为实测；系统接口错误比例被标为实测端到端丢包；RTT 的 0.1 ms 占位值被 warning 错报为 0.0。 | 已修正四类事件为 fallback、接口错误比例为 estimated，并披露实际占位值。`netstat-provenance-red.log`、`pcap-provenance-red.log` 和 `counter-estimate-red.log` 保存旧行为失败，最终完整复验进行中。 |

后续确认的 bug 追加到本节。每项保留受影响版本/平台、期望与实际行为、复现输入、
根因位置、修复文件或提交、验证命令和结果、证据路径及剩余限制。

## 必须复核的历史事项

以下 issue 登记时均为开放状态，未标注 bug 标签。它们包含历史功能和验收任务，
是否仍存在产品缺陷须实测，不能机械计为 5 个已确认 bug。

| 来源 | 待核实内容 | 关闭所需证据 | 当前状态 |
| --- | --- | --- | --- |
| [#1](https://github.com/billlza/netdiag-twin/issues/1) | Overview 五卡等尺寸、圆角、文字/图标视觉居中和边缘裁切。 | macOS 支持窗口尺寸、缩放和中英文下的实际界面检查；相关布局回归。 | 待复核 |
| [#2](https://github.com/billlza/netdiag-twin/issues/2) | 可替换模型、manifest、训练、fallback 披露、HIL 反馈导出。 | 真实模型读写、损坏输入显式失败、训练到诊断再到反馈导出的闭环回归。 | 本机 CLI 闭环通过；模型字节哈希不符和损坏 manifest 在 run 发布前拒绝。模型质量仅针对合成夹具，见验证记录。 |
| [#3](https://github.com/billlza/netdiag-twin/issues/3) | OTLP、原生 pcap/pcap 文件、系统计数器与 provenance。 | 各受支持采集入口的实际输入、错误分类、超时/取消/资源边界及质量证据；设备验收单列。 | OTLP 真实 loopback gRPC、pcap 文件与截断拒绝通过；D-04/D-05 修复复验中。真实网卡采集和实验室设备验收仍缺。 |
| [#4](https://github.com/billlza/netdiag-twin/issues/4) | 拓扑 JSON/YAML 导入导出、校验、快照、策略 what-if。 | 往返保真、非法拓扑拒绝、策略计算和 before/after 验证；界面操作与 CLI 一致。 | 核心往返/非法输入回归、CLI 策略计算及 Pilot after-run 闭环通过；原生 GUI 导入导出待验收。 |
| [#5](https://github.com/billlza/netdiag-twin/issues/5) | DMG、签名/公证、Sparkle、GitHub Release、Homebrew。 | 同一候选的包哈希、签名/公证/安装结果；发布后校验 Release/appcast/cask 一致，并从现有正式版执行更新 smoke。 | 待复核 |
| [历史 Dependabot 失败](https://github.com/billlza/netdiag-twin/actions/runs/29423235967) | 2026-07-15、旧提交 `d6745e9a16e37b61a2d297d805f98c3111ccdf78` 的 `Run Dependabot` 失败。 | 确认更新器失败原因、是否仍影响当前依赖更新及恢复结果；旧日志不能证明当前产品运行失败。 | 旧 SDK 依赖范围阻止安全更新；登记基线 0.5.3 已为 SDK 0.32.1，本次候选保留此修复并通过新 advisory 检查。未把历史调度任务本身标为已重跑。 |

如果复核发现 bug，登记为 D 项并完成修复；如果已有实现且验收通过，保留关闭证据；
如果属于功能请求、重复项或既定支持范围，引用契约和代码说明分类理由。
待复核项未结清前，不能宣称“所有现有已知 bug 已解决”。

## 已观察到的能力与验收缺口

这些缺口也必须在本版复核，不能隐藏或伪装成成功。当前材料把它们定义为安全边界、
能力限制或证据不足，尚不能直接断言它们都是产品 bug。若实际行为违背现有承诺，
须转为 D 项修复；不得通过事后缩减承诺把已确认 bug 改名为限制。

| 编号 | 当前事实与来源 | 后续要求 |
| --- | --- | --- |
| L-01 | Windows 缺少受审查的目录持久化/发布边界，数据集写入、模型训练/重建、HIL、run 创建和历史清理受限。见[平台契约](getting-started.md#platform-mutation-contract)、[模型发布](model-bundle-publication.md)及 `crates/netdiag-platform/src/atomic_directory.rs`。 | 核对所有入口和对外承诺；如实现支持，必须提供原生 Windows 的崩溃恢复、并发、不覆盖和无副作用失败证据，不能使用空 flush 或默认成功。 |
| L-02 | Windows Python 适配器因解释器/祖先目录信任与进程控制边界尚未完成而禁用。见 `crates/netdiag-core/src/python_runtime.rs` 和 `crates/netdiag-core/src/pilot/adapter_contract/process.rs`。 | 核对支持范围和错误反馈；如实现支持，验证 DACL、身份绑定、超时、取消、进程回收和输出上限。 |
| L-03 | [tc/netem 适配器](../examples/adapters/tc-netem-lab/README.md)只输出只读计划，未提供已验证的主动 qdisc 修改与崩溃安全回滚。 | 核对用户可见能力与实际行为；主动网络修改须有明确授权和可靠回滚，不能为消除限制而直接放开。 |
| L-04 | [真实设备状态](real-device-pilot-readiness.json)为 `pending_lab_access` / `not_validated`，`real_device_release_claim_eligible=false`。 | 取得真实设备采集、after-run 验证和已审阅证据前，不声称物理设备验收通过；任何因此无法验证的具体 bug 保持未关闭。 |

## 历史审查问题的当前复核

- PR #8 的 [vendored Cargo.lock 缺失意见](https://github.com/billlza/netdiag-twin/pull/8#discussion_r3587936966)：
  本次在登记基线运行 `python3 scripts/check_patch_contract_hygiene.py`，通过
  （2 个本地补丁）；该旧失败未在当前检查中复现。
- PR #8 的 [architecture guard 路径缺失意见](https://github.com/billlza/netdiag-twin/pull/8#discussion_r3587936970)：
  本次运行 `bash scripts/check_architecture_guard.sh`，退出码 0，路径/行数和
  依赖方向检查通过；该旧失败未在当前检查中复现。

保留这两项的回归检查；上述结果不证明其他问题已经解决。

## 0.5.4 发布验收

1. 完成已知问题来源复核，所有已确认 bug 的未关闭数量为 **0**，所有历史待复核项
   均有可审查结论。优先级低、难复现、外部阻塞或缺少设备均不能自动关闭 bug。
2. 每个修复都有相关回归证据；尽可能保存旧实现失败、新实现通过的结果。
   无法执行旧版时写明原因并提供等效的可达路径/输入证据，不虚称 red/green。
3. 按[质量门禁](quality-gates.md)执行 strict gate，并完成受影响的原生平台、
   采集到诊断到报告/证据导出、持久化/恢复和 GUI 实际交互验证。
   不降低断言、覆盖率或 warning 要求；不能用 mock 代替受影响的真实边界。
4. 按[发布流程](release-process.md)升级候选版本到 `0.5.4`，验证该精确 main
   提交的 CI 和同一候选的签名、公证、安装证据后，才能推送 `v0.5.4` tag。
5. 发布后核对 Release、Sparkle appcast 和 Homebrew 指向同一版本和制品，完成
   旧版更新 smoke；这些结果完成前不宣布本次发布交付完成。
6. Release notes 根据实际关闭的 D 项及验收结果编写，保留能力边界和未完成的
   真实设备验收状态。不得直接把本计划改写成“所有 bug 已修复”的公告。

执行阶段已完成依赖迁移与上述缺陷修复。`strict-06-macos-isolation.log` 完整通过，
后续真实 CLI 复核进一步确认 D-05 的 pcap/proxy 问题，修复后的
`strict-07-provenance.log` 正在完整复验；不能用前一轮通过替代最新候选验证。
原生界面、签名打包安装和旧版更新验收仍待完成。
原始验证记录保存于 `target/verification/v0.5.4-20260912/`，后续结果持续追加。
命令、结果与验收边界见[验证记录](v0.5.4-validation.md)。

## 执行环境与发布阻塞（2026-09-13）

- 当前源目录及 Desktop 为 0775；strict 的 Pilot 正向测试被既有执行目录信任检查拒绝。
  保留了这次失败与源目录权限，未放宽验证。改用 `/Users/bill/netdiag-054-validation.mw3r0qyq`
  的私有隔离 worktree 验证相同候选；补丁 SHA-256 和快照来源存于
  `target/verification/v0.5.4-20260912/trusted-validation-root.json`。
- GitHub 当前只列出 `github-pages` 环境，未配置 `release-signing`、`release-publication`
  和 `release-homebrew`；ruleset 列表为空。Immutable Releases 已启用。
  缺少流程要求的发布保护与独立审核，不能推送发布 tag。
- 当前本机安装的是 0.5.0；0.5.4 调试包已构建、验证 ad-hoc 签名并启动，
  Sparkle 为 2.9.6。此结果不是 Developer ID 签名、公证、正式安装或更新验收。
- 原生候选按现有信任契约拒绝旧数据目录的 0775 权限。53 个目录的精确权限清单
  和内容备份已完成，收紧至 0755 的现有权限变更等待用户明确授权；没有修改 Desktop
  或旧数据权限，也没有降低产品校验。
- macOS 并行启动 nextest 时，两个不启动子进程的测试曾被报告输出管道泄漏。
  50 次隔离执行和 5 轮串行对照通过，5 轮并行邻近测试中复现一次；上游 Rust/nextest
  的管道创建路径支持进程启动时继承的归因。macOS 改为串行测试进程，保留测试内部并发、
  200 ms 泄漏失败标准和全部断言，无自动重试。完整门禁随后通过。
- [依赖升级与兼容约束](v0.5.4-dependencies.md)记录了版本来源、无法直接跨越的上游约束
  和已通过的依赖检查。

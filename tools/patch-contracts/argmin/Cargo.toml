[package]
name = "netdiag-argmin-patch-contract"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[package.metadata.netdiag-patch-contract]
patches = ["argmin"]

[features]
rayon = ["dep:rayon", "argmin/rayon"]

[dependencies]
argmin = "=0.11.0"
rayon = { version = "1.6", optional = true }

[lints]
workspace = true

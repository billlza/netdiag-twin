# 0.5.4 候选验证记录

日期：2026-09-13。本机完整代码门禁通过，发布验收不完整，不能发布。

源分支为 `lza/v0.5.4-known-bugs-and-dependencies`，基于 main
`968e9bb051311655920234c29e961cfdac571bac`。完整 strict 与最终原生采集器验证绑定源提交
`6584025a93346fcf425366e4070596263aa02aef`；后续仅调整 CI 工具链目录与本验证记录，
产品代码保持一致。本机验证在私有 worktree
`/Users/bill/netdiag-054-validation.mw3r0qyq` 中进行；源目录和隔离目录的
`git diff --binary --full-index HEAD` 逐字节一致，源提交与暂存树相等的记录在
`target/verification/v0.5.4-20260912/strict-08-source-receipt.json`。
后续文档记录独立更新，最终提交仍须原生 CI 验证。

## 实际检查

以下路径相对于隔离 worktree；源目录保留早期失败与来源资料，没有覆盖失败尝试。

| 检查 | 实际结果与证据 |
| --- | --- |
| `scripts/check_rust_quality.sh strict`，Rust 1.98.1，`RUSTFLAGS=-D warnings` | 最终 `strict-08-provenance-shared.log` 退出 0：1,149 个 Rust 测试和 206 个 Python 检查通过；依赖、复杂度、架构、适配器、性能、Pilot/calibration/model-gate 均通过。06 轮的通过和 07 轮的单函数复杂度失败仍保留；共享事件披露修正后的结果以 08 轮为准。 |
| 覆盖率 | 08 轮的核心/CLI/平台覆盖率 82.54%（31,914/38,663），release-critical 96.41%（4,510/4,678）；应用安全聚合覆盖率 86.07%（1,402/1,629），逐文件下限通过。没有修改覆盖率标准。 |
| Overview red/green | 宽度旧实现失败于 `overview-width-red.log`；滚动旧实现失败于 `overview-scroll-red-03.log`；5 项修复后回归通过于 `overview-layout-green-02.log`。这是几何/滚动测试，不是原生截图验收。 |
| 系统计数器解析 red/green | `netstat-format-red-02.log` 证明别名行与空 Address 缺陷；`netstat-format-green.log` 中 17 项通过。`all` 和 `lo0` 的真实 CLI 采集成功，最终结果为 `system-counters-{all,lo0}-final-02.json`。 |
| 指标质量 red/green | `netstat-provenance-red.log`、`pcap-provenance-red.log`、`counter-estimate-red.log` 保留旧质量标注失败。最终 strict 通过，真实系统计数器与合成 pcap 的 CLI 均为 1 measured / 1 estimated / 8 fallback；见 `native-collector-final-receipt.json`。 |
| `cargo test --locked -p netdiag-app --lib website_probe_can_collect_default_public_targets -- --ignored --nocapture` | 主动公网 smoke 单独通过；默认测试仍显式忽略这项外网检查。未修改代理、DNS、路由或 TUN。 |
| CLI 模型到反馈闭环 | `cli-acceptance-01/receipt.json` 记录独立进程命令：初始化私有根、JSONL 训练、CSV 诊断、合成夹具 HIL 记录、反馈导出/校验、拓扑与策略验证、what-if、证据导出；输出 1 行反馈、含 15 文件的 ZIP，ZIP 完整性检查通过。 |
| CLI 损坏输入 | 截断 pcap、模型字节哈希不符、非法 manifest 均显式失败；后两者没有发布 run。记录在同一 CLI receipt 及 stderr 中。 |
| OTLP 边界 | `generated_grpc_client_exports_and_snapshots_a_complete_observation` 使用生成的 gRPC 客户端和真实 loopback socket；完整观测、连接释放及 graceful shutdown 断言通过，部分 HTTP/2 连接的强制有界回收另有回归。 |
| pcap 文件边界 | 独立 CLI 读取含合法 Ethernet/IPv4/TCP 帧的合成 classic-pcap，截断版本失败；现有加载/取消/期限/符号链接/容量测试通过。这不是物理网卡采集证据。 |
| 依赖、脚本和工作流 | 最新 advisory 数据库结果为 advisories/bans/licenses/sources 全部通过，无 advisory waiver。Shellcheck、Actionlint 与 Python 门禁已通过；最终文档/提交另行复核。 |

CLI 实验保留了两次验证输入修正：大写 `Congestion` 被标签解析器拒绝后，使用合法的
小写值完成闭环；给 manifest 添加合法空白仍可加载，因为格式不对 manifest 来源作认证，
报告记录本次读取的确切哈希。没有将合法空白接受误报为产品缺陷；真正的非法 JSON
与模型哈希不符通过独立目录验证为失败。所有样本/HIL 记录均为隔离合成夹具，未执行网络策略。

## 尚未具备的验收证据

- macOS 原生候选可启动并显示 0.5.4，但旧应用数据目录的 0775 权限被信任检查拒绝。
  精确的 53 目录权限修复清单和备份已完成，现有权限变更待用户授权；有数据的概览、
  中英文与窗口尺寸、GUI 拓扑导入导出尚未完成。
- 已构建的调试包仅通过 ad-hoc `codesign --verify --deep --strict`，不是 Developer ID
  签名、公证、正式安装或 Sparkle 更新验收。最新调试构建号为 `202609130206`，
  包内二进制 SHA-256 为 `8f2838d08a951191f3f43fae3970fc61ff32a5b9dbf7b3e9062c09bba3d99f58`；
  `debug-package-receipt-02.json` 绑定源提交、打包前/后哈希、Sparkle 2.9.6 和签名结果。
- 远端缺少 `release-signing`、`release-publication`、`release-homebrew` 保护环境及
  release tag ruleset，当前协作者列表也没有独立发布审核人。Immutable Releases 已启用。
  未创建发布 tag 或降低发布校验。
- 原生 Windows/Linux CI 已在 `6584025` 通过；同轮 macOS adapter-schema 因 Rustup
  组件文件冲突失败，已用任务独立安装目录修正。同版本的全新目录安装与自动补齐已本机通过。
  最新精确提交的原生 CI 以[PR #9 检查](https://github.com/billlza/netdiag-twin/pull/9/checks)为准。
  Windows 的持久化发布与 Python 适配器限制保留，tc/netem 保持只读；真实设备状态仍为
  `pending_lab_access` / `not_validated`。

已通过的检查仅支持相应范围；上述缺口未补齐前，不能宣布“所有已知 bug 已关闭”或
“0.5.4 已发布”。完整问题范围见[版本台账](v0.5.4-plan.md)。
